{
  "name": "crawler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/index.js\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "config": "*",
    "nodemon": "^2.0.15",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongodb": "^4.4.1",
    "node-fetch": "^3.2.3"
  }
}
